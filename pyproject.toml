[project]
name = "tashan-development-hooks-loop"
version = "0.1.0"
description = "Codex stop hook v-task classifier"
requires-python = ">=3.13"
dependencies = [
  "openai",
  "python-dotenv",
]

[dependency-groups]
dev = [
  "pytest",
]
